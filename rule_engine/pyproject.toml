[project]
name = "rule-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.41.0",
    "requests>=2.34.2",
    "sandbox",
]

[tool.uv.sources]
sandbox = { path = "../sandbox" }
